本目录用于存放项目头文件（`.h` / `.hpp`）。

头文件通常包含：
- 对外声明（函数、类、结构体）
- 宏定义与常量定义
- 供多个源文件共享的接口

在 `src` 目录下的源文件中，可以通过 `#include "xxx.h"` 引用这些头文件。

示例：

```c
#include "header.h"

int main(void) {
  // ...
}
```

使用头文件可以避免在多个源文件中重复复制声明，便于维护与统一修改。

更多说明可参考 GCC 文档：
https://gcc.gnu.org/onlinedocs/cpp/Header-Files.html
